# Server-only. Never prefix with VITE_ — that would expose the token in the browser.
# Fine-grained PAT: grant access only to the private repos you want listed.
# Classic PAT: `repo` scope (can see all private repos — prefer fine-grained).
GITHUB_TOKEN=
